/**
 * Single source of truth for the privacy notice version.
 * Increment whenever /adatkezeles changes substantively.
 * The current version is recorded with every consent submission so we
 * can prove what the user agreed to (GDPR Art. 7(1) burden of proof).
 */
export const PRIVACY_POLICY_VERSION = "2.0-2026-04-07";
